import React, {useEffect} from 'react';
import {connect} from 'react-redux';
import {
  StyleSheet,
  SafeAreaView,
  View,
  ActivityIndicator,
  Text,
} from 'react-native';

const ChatsListScreen = () => {

  

  return (
    <SafeAreaView>
      <Text>Hello, ChatsList</Text>
    </SafeAreaView>
  );
};

export default ChatsListScreen;
