import React, {useEffect} from 'react';
import {connect} from 'react-redux';
import {
  StyleSheet,
  SafeAreaView,
  View,
  ActivityIndicator,
  Text,
} from 'react-native';

const ContactsListScreen = () => {
  return (
    <SafeAreaView>
      <Text>Hello, ContactsList</Text>
    </SafeAreaView>
  );
};

export default ContactsListScreen;
